# UI/UX 設計書

作成日: 2025-11-23

## デザインコンセプト

- **シンプル & モダン**: 最小限のUIで最大の機能性
- **iOS ネイティブ**: iOS デザインガイドラインに準拠
- **直感的**: 説明不要で使えるインターフェース
- **アクセシビリティ**: Dynamic Type、VoiceOver 対応

## カラーパレット

```swift
// Primary Colors
- Accent Color: システムブルー (.blue)
- Background: システム背景色 (.systemBackground)
- Secondary Background: .secondarySystemBackground
- Tertiary Background: .tertiarySystemBackground

// Semantic Colors
- Success: システムグリーン (.green)
- Warning: システムオレンジ (.orange)
- Error: システムレッド (.red)
- Text Primary: .primary
- Text Secondary: .secondary
```

## タイポグラフィ

```swift
- Large Title: .largeTitle (34pt, Bold)
- Title: .title (28pt, Bold)
- Title 2: .title2 (22pt, Bold)
- Title 3: .title3 (20pt, Semibold)
- Headline: .headline (17pt, Semibold)
- Body: .body (17pt, Regular)
- Callout: .callout (16pt, Regular)
- Subheadline: .subheadline (15pt, Regular)
- Footnote: .footnote (13pt, Regular)
- Caption: .caption (12pt, Regular)
```

## 画面設計

### 1. カード集一覧画面 (CollectionsListView)

**目的**: すべてのカード集を表示し、選択・作成・削除を行う

```
┌─────────────────────────────┐
│ < 設定          カード集    │ Navigation Bar
├─────────────────────────────┤
│                             │
│  ┌─────────────────────┐   │
│  │ 📚 日常会話         │   │ Collection Card
│  │ 20 cards            │   │
│  │ 2025/11/20          │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 📚 ビジネス英語     │   │
│  │ 15 cards            │   │
│  │ 2025/11/18          │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 📚 旅行フレーズ     │   │
│  │ 8 cards             │   │
│  │ 2025/11/15          │   │
│  └─────────────────────┘   │
│                             │
│              [ + ]          │ Floating Action Button
└─────────────────────────────┘
```

**UI要素**:
- Navigation Bar: タイトル「カード集」、設定ボタン
- List: カード集のカード (swipe-to-delete 対応)
- Card: アイコン、タイトル、カード数、作成日
- FAB (+): 新規カード集作成

**インタラクション**:
- タップ → カード一覧画面へ遷移
- スワイプ左 → 削除ボタン表示
- + ボタン → カード集作成シート表示

---

### 2. カード一覧画面 (CardsListView)

**目的**: 選択したカード集内のカードを一覧表示、検索、学習開始

```
┌─────────────────────────────┐
│ < 戻る     日常会話      … │ Navigation Bar
├─────────────────────────────┤
│ 🔍 カードを検索...         │ Search Bar
├─────────────────────────────┤
│                             │
│  ┌─────────────────────┐   │
│  │ おはよう            │   │ Card Row
│  │ Good morning        │   │
│  │ #挨拶               │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ ありがとう          │   │
│  │ Thank you           │   │
│  │ #挨拶               │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ お疲れ様です        │   │
│  │ Good job            │   │
│  │ #ビジネス           │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │    学習モード       │   │ Learn Button
│  └─────────────────────┘   │
│              [ + ]          │ FAB
└─────────────────────────────┘
```

**UI要素**:
- Navigation Bar: 戻るボタン、カード集名、メニューボタン (並び替え、削除)
- Search Bar: リアルタイム検索
- List: カードのリスト (日本語・英語・タグ表示)
- 学習モードボタン: フラッシュカード学習開始
- FAB (+): 新規カード作成

**インタラクション**:
- タップ → カード編集画面
- スワイプ左 → 削除
- 学習モードボタン → LearnModeView へ遷移
- + ボタン → カード作成画面へ遷移

---

### 3. カード作成・編集画面 (CreateEditCardView)

**目的**: 日本語入力、AI候補生成、英語選択・編集、保存

```
┌─────────────────────────────┐
│ キャンセル  カード作成  保存│ Navigation Bar
├─────────────────────────────┤
│                             │
│  日本語 *                   │
│  ┌───────────────────────┐ │
│  │ よろしくお願いします  │ │ Text Field
│  └───────────────────────┘ │
│                             │
│  ┌─────────────────────┐   │
│  │ 💡 候補を生成       │   │ AI Button
│  └─────────────────────┘   │
│                             │
│  英語 *                     │
│  ┌───────────────────────┐ │
│  │ Nice to meet you     │ │ Text Field (editable)
│  └───────────────────────┘ │
│                             │
│  AI候補:                    │
│  ┌───────────────────────┐ │
│  │ ✓ Nice to meet you   │ │ Selected
│  └───────────────────────┘ │
│  ┌───────────────────────┐ │
│  │ ○ Pleased to meet you│ │ Unselected
│  └───────────────────────┘ │
│  ┌───────────────────────┐ │
│  │ ○ Best regards       │ │
│  └───────────────────────┘ │
│                             │
│  メモ (任意)                │
│  ┌───────────────────────┐ │
│  │ ビジネスシーンで使用 │ │
│  └───────────────────────┘ │
│                             │
│  タグ (任意)                │
│  ┌───────────────────────┐ │
│  │ 挨拶, ビジネス       │ │
│  └───────────────────────┘ │
│                             │
└─────────────────────────────┘
```

**UI要素**:
- Navigation Bar: キャンセル、タイトル、保存ボタン
- Form:
  - 日本語入力フィールド (必須)
  - 候補生成ボタン (AI icon)
  - 英語入力フィールド (必須、編集可能)
  - AI候補リスト (タップで選択)
  - メモフィールド (任意)
  - タグフィールド (任意、カンマ区切り)

**インタラクション**:
1. 日本語入力
2. 「候補を生成」ボタンタップ → ローディング → 候補表示
3. 候補をタップ → 英語フィールドに自動入力
4. 英語を手動編集可能
5. 保存ボタン → バリデーション → 保存 → 一覧画面に戻る

**バリデーション**:
- 日本語と英語は必須
- 空白のみの入力は不可
- エラーメッセージを赤文字で表示

---

### 4. 学習モード画面 (LearnModeView)

**目的**: フラッシュカード形式でカードを学習

```
┌─────────────────────────────┐
│ × 閉じる    学習モード      │ Navigation Bar
├─────────────────────────────┤
│                             │
│         3 / 20              │ Progress Indicator
│    ━━━━━━━━━━━━━━━━━━━    │ Progress Bar
│                             │
│                             │
│                             │
│    ┌─────────────────┐     │
│    │                 │     │
│    │   おはようございます   │ Card (Front)
│    │                 │     │
│    │                 │     │
│    └─────────────────┘     │
│                             │
│                             │
│    ┌─────────────────┐     │
│    │   答えを見る    │     │ Reveal Button
│    └─────────────────┘     │
│                             │
│                             │
│    [＜ 前へ]     [次へ ＞]  │ Navigation Buttons
│                             │
└─────────────────────────────┘

(答えを見た後)
┌─────────────────────────────┐
│ × 閉じる    学習モード      │
├─────────────────────────────┤
│         3 / 20              │
│    ━━━━━━━━━━━━━━━━━━━    │
│                             │
│    ┌─────────────────┐     │
│    │  おはようございます   │ Card (showing both)
│    │                 │     │
│    │  ───────────    │     │
│    │                 │     │
│    │  Good morning   │     │
│    └─────────────────┘     │
│                             │
│    ┌─────────────────┐     │
│    │   もう一度      │     │ Flip Back Button
│    └─────────────────┘     │
│                             │
│    [＜ 前へ]     [次へ ＞]  │
│                             │
└─────────────────────────────┘
```

**UI要素**:
- Navigation Bar: 閉じるボタン、タイトル
- Progress: カウンター (現在/総数) とプログレスバー
- Card: 大きなカード表示
  - 表面: 日本語のみ
  - 裏面: 日本語 + 区切り線 + 英語
- ボタン:
  - 答えを見る / もう一度 (トグル)
  - 前へ / 次へ (ナビゲーション)

**インタラクション**:
- 「答えを見る」タップ → アニメーションで英語表示
- 「もう一度」タップ → 日本語のみに戻る
- スワイプ左右 → 前後のカード移動
- 最後のカード → 「完了」ボタン表示

---

### 5. 設定画面 (SettingsView)

**目的**: AI候補数、その他の設定を変更

```
┌─────────────────────────────┐
│ < 戻る        設定          │ Navigation Bar
├─────────────────────────────┤
│                             │
│  AI設定                     │ Section Header
│  ┌─────────────────────┐   │
│  │ 候補数              │   │
│  │            3    ＞  │   │ Picker (1-5)
│  └─────────────────────┘   │
│                             │
│  データ                     │
│  ┌─────────────────────┐   │
│  │ カード総数          │   │
│  │            43       │   │ Read-only
│  └─────────────────────┘   │
│  ┌─────────────────────┐   │
│  │ カード集数          │   │
│  │            3        │   │ Read-only
│  └─────────────────────┘   │
│                             │
│  アプリ情報                 │
│  ┌─────────────────────┐   │
│  │ バージョン          │   │
│  │            1.0.0    │   │
│  └─────────────────────┘   │
│  ┌─────────────────────┐   │
│  │ プライバシーポリシー│   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

**UI要素**:
- Navigation Bar: 戻るボタン、タイトル
- Form / List:
  - AI設定セクション: 候補数ピッカー
  - データセクション: 統計情報（読み取り専用）
  - アプリ情報セクション: バージョン、ポリシー

---

## コンポーネントライブラリ

### 共通コンポーネント

#### 1. CollectionCard
```swift
// カード集カード
VStack(alignment: .leading) {
    HStack {
        Text("📚")
        Text("日常会話").font(.headline)
    }
    Text("20 cards").font(.subheadline).foregroundColor(.secondary)
    Text("2025/11/20").font(.caption).foregroundColor(.secondary)
}
.padding()
.background(Color.secondarySystemBackground)
.cornerRadius(12)
```

#### 2. WordCardRow
```swift
// 単語カード行
VStack(alignment: .leading, spacing: 4) {
    Text("おはよう").font(.body)
    Text("Good morning").font(.subheadline).foregroundColor(.secondary)
    Text("#挨拶").font(.caption).foregroundColor(.blue)
}
.padding(.vertical, 8)
```

#### 3. AIButton
```swift
// AI候補生成ボタン
Button(action: generateCandidates) {
    HStack {
        Image(systemName: "lightbulb.fill")
        Text("候補を生成")
    }
    .frame(maxWidth: .infinity)
    .padding()
    .background(Color.blue)
    .foregroundColor(.white)
    .cornerRadius(10)
}
```

## アニメーション

- **画面遷移**: デフォルトの NavigationStack transition
- **カード追加**: .transition(.move(edge: .bottom))
- **削除**: .transition(.opacity)
- **学習モードフリップ**: .rotation3DEffect (Y軸回転)
- **候補選択**: .scaleEffect + .animation(.spring())

## アクセシビリティ

- **Dynamic Type**: すべてのテキストで対応
- **VoiceOver**: すべてのボタンとフィールドにラベル
- **Color Contrast**: WCAG AA 基準準拠
- **Tap Targets**: 最小 44x44 pt

## エラー状態

### エラーメッセージ表示
```swift
// Alert または Toast
"日本語と英語は必須です"
"AI候補の生成に失敗しました"
"カードの保存に失敗しました"
```

### 空状態
```swift
// カード集が空
VStack {
    Image(systemName: "tray")
        .font(.system(size: 60))
        .foregroundColor(.secondary)
    Text("カード集がありません")
        .font(.headline)
    Text("＋ボタンで新しいカード集を作成しましょう")
        .font(.subheadline)
        .foregroundColor(.secondary)
}
```

## ダークモード対応

- すべての色はシステムカラーを使用
- 自動的にライト/ダークモード対応
- カスタムカラーは両モードで視認性を確保

---

この設計により、シンプルで使いやすく、iOS ネイティブな体験を提供します。
